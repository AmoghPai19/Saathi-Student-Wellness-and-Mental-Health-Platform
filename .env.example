# Frontend (Vite) environment
# Backend origin (no trailing /api). Set to your deployed backend.
# Example:
# VITE_API_URL=https://saathi-student-wellness-and-mental-two.vercel.app
VITE_API_URL=

# Dev server port (optional)
# VITE_PORT=8080

# Backend (Express) environment
# NOTE: Do NOT commit real secrets in this file. Use Backend/.env for real values.

# Port for Express server
PORT=5000

# Allowed CORS origin for production (your deployed frontend URL)
# Example:
# CORS_ORIGIN=https://your-frontend.vercel.app
CORS_ORIGIN=

# Mongo connection string
MONGODB_URI=

# JWT secret for signing tokens
JWT_SECRET=